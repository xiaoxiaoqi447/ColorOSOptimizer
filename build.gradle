buildscript {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://api.lsposed.org' }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://api.lsposed.org' }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
